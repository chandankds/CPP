#include<iostream>
/*
	-> lazy copy is combination of shallow copy and deep copy and take advantage of the concept of reference counting and 
		copy on write. idea is to make copy only when the object is modified and get rid of object when reference count reaches 0;
	-> Will try implementing it later coz its a bit complex. its in lecture 12 at last 45 minutes.
*/
int main() {

	return 0;
}