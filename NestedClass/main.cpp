#include<iostream>

/*
	-> A class defined a function is called local class and it cannot be accesed outside the function by pointer reference or anything 
		I think because of the function activation record on the stack.
	-> When we define the class inside the scope of another class it is called nested class.
	-> Implemet a linklist class having node as a nestes class.
	-> Wen can declare nested class as private public and protected.
	-> If you want to access the members of inner class inside the outer class you have to decalre outer class as friend inside the inner class.
	-> 
*/

int main() {

	return 0;
}